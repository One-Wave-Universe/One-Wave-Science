# Musical Universe — Canonical Status

Version: Updated 40 Canonical Correction
Date: July 27, 2026
Status: YELLOW One-Wave interpretation over GRAY music theory and acoustics

## Locked Corrections

The following rules are canonical for the active Musical Universe material:

1. A selected root is the local anchor at `0`.
2. `+1` through `+5` and `-1` through `-5` are mirrored route coordinates.
3. `+6` and `-6` are the same opposite pitch class and are written `±6`, the shared Mirror position.
4. Positive and negative signs identify route direction only.
5. Clockwise and counterclockwise do not permanently mean expression and compression.
6. Compression and expression, when used, describe movement relative to the active anchor or Field.
7. Chord Rotation preserves the full coordinate set of the chord. It does not automatically reduce the chord to a compression/expression pair.
8. The former chord-lean formula is retired.

## Open Yellow Questions

The following are active questions, not canonical mechanisms:

- whether the `±5` fifth/fourth positions behave as reinforced boundaries
- whether sounding a boundary promotes it into a new local Field
- whether paired boundary routes interfere, balance, reinforce, or cancel
- whether diminished harmony holds two competing resolution directions at `±6`
- whether any coordinate interaction predicts measured phase, amplitude, or listener response

Literal acoustic cancellation is never inferred from clock geometry alone. It requires compatible frequencies, amplitudes, timing, and opposite phase.

The public presentation format remains unresolved. Preserve the material without forcing it into a textbook, wiki, newspaper, or field-manual style until that choice is made.
