[project]
name = "physics-engine"
version = "0.1.0"
description = "One-Wave Physics Engine: canon wave/field mathematics made runnable and tested"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
