name: tmp-vbb-bjt-limiter-convergence
on:
  push:
    branches: [virtual-breadboard-bjt-support]
    paths:
      - '.github/workflows/tmp-vbb-bjt-limiter-convergence.yml'
      - '.github/scripts/fix_vbb_bjt_limiter_convergence.py'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: virtual-breadboard-bjt-support
      - run: python .github/scripts/fix_vbb_bjt_limiter_convergence.py
      - run: |
          git config user.name "Mark Wright"
          git config user.email "markluvsoliviaduh@gmail.com"
          git add Virtual_Breadboard/js/circuit.js
          git commit -m "Virtual Breadboard: gate BJT convergence on junction limiting"
          git push origin HEAD:virtual-breadboard-bjt-support
