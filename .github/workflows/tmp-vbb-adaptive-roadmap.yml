name: tmp-vbb-adaptive-roadmap
on:
  push:
    branches: [virtual-breadboard-adaptive-transient]
    paths:
      - '.github/workflows/tmp-vbb-adaptive-roadmap.yml'
      - '.github/scripts/mark_vbb_adaptive_done.py'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: virtual-breadboard-adaptive-transient
      - run: python .github/scripts/mark_vbb_adaptive_done.py
      - run: |
          git config user.name "Mark Wright"
          git config user.email "markluvsoliviaduh@gmail.com"
          git add Virtual_Breadboard/SPICE_PARITY.md
          git commit -m "Virtual Breadboard: mark adaptive transient implemented"
          git push origin HEAD:virtual-breadboard-adaptive-transient
