# Quadratic + top layer

Same cell. Extra floor is not a fourth winding.

```
TOP     Field lists lean     Void may OVERRIDE engage=0
        oversight watches all three gates (6:1)
        HOLD from here means: no new lean, memory stays

QC      views UP     memristor / Hall / RC-MEM = what B and G are now
        actions DOWN windings / torque / spin = what the body just did
        one flip, one seq

CELL    DC AC RC, three windings, star = G, I_0 vagus
```

Override is Void cutting engage. It is not Gate-7. It does not stamp.
Hold from the top is STAY on all windings plus leftover view state.
First view device on the bench: Hall at the star, or the Stage-1 RC MEM. A discrete memristor drops in later as the same UP node.
