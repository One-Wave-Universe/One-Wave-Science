# Three-rail virtual ground

```
+ rail   forward DC
0 rail   virtual ground / return / receipt
− rail   backward DC
```

`G` is made. Not earth. Stiff both ways. Finite hold window.
Measure `I_G` at VGBUF. If `+1+−1` knocks `V_G` out of the belt, the mid is not Ground.
Scope earth must not be bonded to NET_G.
See HEX-SPLIT `GROUND.md` for the lattice name of this node.
