# UPDATED 35 — Five Field Lifecycle States

**Status:** Current canonical correction for the five Field states.

## Correction

The five Field states are behavioral lifecycle states:

```text
IDLE -> PRIMED -> EXECUTING -> VECTORING -> RESOLVING
```

Definitions:

1. **Idle** — no committed action; stable/reference condition.
2. **Primed** — potential is loaded and conditions are ready, but action has not fired.
3. **Executing** — the selected action is actively being performed.
4. **Vectoring** — active motion/action is being steered, redirected, or oriented by feedback.
5. **Resolving** — the consequence is settling into a stable next state/reference.

The lifecycle closes as:

```text
IDLE -> PRIMED -> EXECUTING -> VECTORING -> RESOLVING -> IDLE / next PRIMED
```

## State is not scale

These five Field states are not the five modulation/scale levels.

Five modulation/strength levels remain a separate axis and may still be encoded neutrally as:

```text
-2 -1 0 +1 +2
```

Scale labels such as:

```text
Floor / Low / Middle / High / Ceiling
Micro / Small / Medium / Large / Macro
```

are modulation/scale representations. They must not be substituted for the Field lifecycle states.

## Supersession rule

Where `UPDATED_33_INVARIANT_ENGINE_VTC_BUILD_AND_VIEW_ACTION_CORRECTION.md`, `AI_CANONICAL_START_HERE.md`, or older files describe the five Field states as only `-2,-1,0,+1,+2` or as scale names, that wording is superseded by this correction.

The invariant six-pair oscillator, six process steps, four Views, four Actions, two engagement choices, and three differential moves are unchanged by this correction.
