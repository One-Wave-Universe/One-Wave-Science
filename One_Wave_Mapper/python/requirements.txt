numpy>=1.24
scipy>=1.10

# Optional: only needed for live hardware capture (device_manager.py).
# sounddevice>=0.4

# Optional: only needed for the future PySide6/pyqtgraph GUI (Section 3.3).
# PySide6>=6.5
# pyqtgraph>=0.13

# Dev/test
pytest>=7.0
