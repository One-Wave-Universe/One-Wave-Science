numpy>=1.24
scipy>=1.10

# Oscilloscope UI (onewave_mapper.gui / onewave_mapper.ui.*)
PySide6>=6.5
pyqtgraph>=0.13

# Optional: only needed for live hardware capture (device_manager.py).
# Requires PortAudio on the system; uncomment on a machine with a real
# microphone/interface.
# sounddevice>=0.4

# Dev/test
pytest>=7.0
