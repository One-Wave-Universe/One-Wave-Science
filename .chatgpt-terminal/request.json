{
  "id": "repo-cleanup-inventory-20260924-01",
  "argv": [
    "bash",
    "-lc",
    "printf 'HOST='; hostname; printf '\\nUSER='; whoami; printf '\\nHOME='; printf '%s\\n' \"$HOME\"; printf '\\nREPOS\\n'; find \"$HOME\" -maxdepth 2 -type d \\( -name '.git' -o -name 'One-Wave-*' -o -iname '*build*' -o -iname '*bench*' -o -iname '*myth*' \\) -print 2>/dev/null | sed 's#/.git$##' | sort -u | head -200"
  ],
  "timeout": 30,
  "intention": "Inventory local Science, Builds, Bench, and Mythos repository paths before moving any non-science content.",
  "consequence": "Read-only filesystem/repository discovery; no files changed or deleted."
}
