> **House: One-Wave-Science.** Sims and lab notes. Not the grant cell.

# GRAV LAB — OCTAVE CASCADE

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python sim/octave_cascade.py
```
