# Codex — Latest Lattice Handoff

For any SCLFS, formatted-drive lattice, Miniverse, lattice movement, frame binding, mirror/orientation, or lattice verification task, read this file first:

- `UPDATED_50_SCLFS_LATTICE_FRAME_BINDING_VERIFICATION_AND_MINIVERSE_RUNTIME.md`

Then read the existing canonical/operations references it names.

Do not treat the repository copy of `miniverse_mud` as the live Miniverse. Source/tests may live in Git; the intended live world/process state belongs on the Jetson external storage.

Current key contract:

```text
fixed authoritative lattice
        +
movable bound X/Y/Z local frame
        +
legal topology-aware traversal
        +
exact shadow/mimic mode
        +
independent verification
        +
shared multi-AI Miniverse state
```

Do not reformat physical drives merely to implement this contract. Inspect the current formatter/runtime first and identify what is already encoded versus what is actually missing.
