[project]
name = "council-chamber"
version = "0.1.0"
description = "One-Wave Council Chamber: shared multi-AI chat and coding workspace"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
