# Translator variant atlas branch-step

MAIN GOAL: reliable Field/Void software construction and system translation.

WHY: capture the user's complete signed rabbit-hop proposal as reproducible mappings and graphs for workers, without losing operation order or confusing it with physical theory.

CURRENT STEP: additive source/clarification record, exact rational explorer, tests and standalone XY atlas.

HARD START: main fetched at 7fc4172; locked translator grammar and both prior branch-step handoffs read; new worktree verified.

ROOT: canonical /home/Scales/One-Wave-Science; worktree /tmp/one-wave-translator-atlas; branch codex/translator-variant-atlas.

REFERENCES: AGENTS.md, JETSON_OPENCLAW_RUNTIME.md, BRANCH_STEP_PROJECT_TEMPLATE.md, RABBIT_HOPPING_ADDRESS_TRANSLATOR_LOCK.md, ARCHITECTURE_RABBIT_HOPPING_SCALE_TRANSLATOR.md, G-721, existing alphabet/rail code and tests, current user correction.

ALLOWED FILES: docs/rabbit_hop_variants/*, RABBIT_HOPPING_VARIANT_ATLAS.md, this packet.

PROTECTED: existing runtime three-route enum and bounded division rail, all SCLFS code and devices, Terminal Bridge, other branches.

EXACT ACTION: map 2N, odd-up alias 2N+1, 2N+K, 2(N+K), N/2, (N-K)/2 and N/2-K with both wrappers, polarity, label/vertical inversion and reverse traversal. Graph whole horizontal runs and tentative bounce separately. No Circle-of-Fifths implementation without its source.

SUCCESS: corrected B gives (2,5,4)/(2,5,6); exact family recovery and operation-order distinctions hold; both signs and domains render; rational results never silently gain integer parity; existing tests remain passing.

TESTS: 10 new unittest cases include signed/oriented wrapper reconstruction; 22 existing alphabet/rail tests pass. Browser verified corrected B, inverted/opposing chromatic selection, intersection overlay and rendered XY plot. Full exact corpus generation covers 120 views at K=1/2/3. GUI permits K through 1000; Python ladder has no such display limit.

FIELD NOTES: user explicitly corrected odd B and said interpret the idea, not typos. Removed literal N-K/2 branch; normalized A wrapper typos under ±1 rule. Kept source verbatim as provenance. Opposing means traversal per translator lock, not SCLFS OPPOSE.

VOID PRE/POST-OVERSIGHT: independent review_guide ALLOW for additive atlas with explicit rational-domain/operation-order/provenance boundaries. Post-review ALLOW; suggestions addressed by signed-wrapper reconstruction test and explicit last-valid-view disclosure for invalid K.

PROGRESS: candidate implemented and tested; standalone preview served from Physics-District public/translators. First regression invocation used incorrect cwd and failed discovery; rerun from verified repo root passed all 22. No code workaround required.

ATTEMPT: approach A, 1/3; no arithmetic test failures.

REFLECTION: clarify transcription once, preserve corrections and provenance, then test a consistent mapping. Unbounded route families require finite display windows. Numeric equality does not erase route identity. Physical interpretation and loop closure remain unproven.

HARD STOP: reviewed atlas committed/published on its branch. No main merge, core route promotion, physical-device change or invented fifths geometry.

HANDOFF: use README equations plus exact corpus for downstream adapters. Rational-to-integer projection needs a separate declared contract. Await circle-of-fifths handoff and loop closure definition.
