---
node_id: "D-418"
canonical_name: "Recursive Planetary Point-Path-Field Rotation Model"
namespace: "NODE"
gate: "YELLOW"
lifecycle: "SUPERSEDED"
classification: "Resonance, Modal & Dimensional Structure / Planetary-Scale Displacement Lineage"
claim_gate_detail: "YELLOW (locked test architecture; coupling functions G_T, F, and the interaction expansion are not yet derived). Superseded by D-419's fuller planetary-displacement statement; retained as the node that fixed the nine-component recursive Point-Path-Field structure and internal rotation bookkeeping."
metadata_standard: "I-06"
---

# Node D-418: Recursive Planetary Point-Path-Field Rotation Model

## Dependencies

Upstream: D-417, A-107, A-109, A-112, A-112a, B-206a, B-206b, C-303,
C-307, C-311, C-313, D-404, D-405.

Superseded by: D-419.

Authority and full engineering contract:
`UPDATED_40_RECURSIVE_PLANETARY_POINT_PATH_FIELD_ROTATION_MODEL.md`.

## Definition

D-418 completes what D-417 left flattened: a planet is not a radial
potential plus optional corrections. Its external orbital behavior
emerges from a compressed 2D bound-lattice state, internal matter and
EM circulation, whole-body spin, orbital/path rotation, external field
rotation, size/density structure, moving finite-range overlap with every
other active body, and EM-shell coupling — interacting, not summed.

## Recursive Point-Path-Field rule

Each outer state (Point, Path, Field) contains its own Point-Path-Field
triad, giving nine components per body:

`R_i = {PP, PPa, PF, PaP, PaPa, PaF, FP, FPa, FF}_i`

- Outer POINT: `PP` internal center/core state, `PPa` internal
  matter/current circulation paths, `PF` field generated by the internal
  point state.
- Outer PATH: `PaP` instantaneous orbital-phase body state, `PaPa`
  trajectory/speed/curvature/angular rate, `PaF` wake/slope geometry
  carried along the orbit.
- Outer FIELD: `FP` local reference center in the surrounding field,
  `FPa` circulation/transport/shear routes through it, `FF` larger-scale
  solar/planetary field geometry and overlap.

No component collapses to one scalar when its internal structure is
dynamically relevant.

## Compressed 2D bound-lattice state

`L2_i = {D_i, C_i, T_i, gamma_i, q_i, phase_i}` (displacement,
compression/binding, tension/stiffness, shear-damping, integrity,
compression/expression phase), with the rule `2D bound state -> point
state -> path state -> field state` and feedback from Path/Field into
the next Point state.

## Internal rotation and shear

Internal layers `Omega_inner, Omega_outer_core, Omega_conducting_fluid,
Omega_mantle, Omega_crust, Omega_bulk, Omega_EM` and relative shears
`DeltaOmega_ab = Omega_a - Omega_b` feed a candidate softening load
`H_shear,i = SUM_ab gamma_ab |DeltaOmega_ab|^2`, modifying effective
stiffness `T_eff,i = T_0,i * G_T(H_shear,i, C_i, q_i)` (G_T undetermined,
not to be tuned per planet). Internal EM rotation is tracked as a
coupled but distinct chain: conducting flow -> current circulation `J`
-> intrinsic `B` -> EM-field rotation/orientation, with
`DeltaOmega_matter_EM = Omega_matter - Omega_EM`. EM modifies shell
integrity/stiffness/response only.

## Unified interaction state and falsification

`X_i = {L2_i, D_i, C_i, T_eff,i, gamma_i, q_i, rho_i(r),
Omega_internal_layers, DeltaOmega_layers, J_i, B_i, Omega_EM,i, the nine
PPaF components, R_active,i, DeltaS_i}`, updated interaction-first:
`X_i(t+dt) = U(X_i(t), {X_j(t) overlaps}, reference_i(t))`, with
`a_i(t) = A_OW(DeltaS_i, L2_i, recursive_PPaF_i, EM_i, overlaps_i)`.
Mercury/Earth/Mars/Venus/giant-planet EM-shell assignments follow
D-417's control structure exactly. Reject the model if it needs a
per-planet arbitrary coefficient, a universal hand-set range, magnetic
moment alone to explain orbit, fictitious Mars/Venus global fields,
hidden memory, flat Point/Path/Field scalars, or Mercury-first fitting
that changes the law for every other planet.

## Disposition

The nine-component recursive Point-Path-Field bookkeeping and the
internal-rotation/shear/EM state carry forward unchanged into D-419,
which restates them as one planetary displacement state and adds tidal,
stress, and stability as further readouts of that same state.
