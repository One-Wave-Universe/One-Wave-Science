---
id: G-756
title: Build contract square-away
status: yellow-architecture
claim_boundary: dictionary and stacking order; does not add kernel axes; millivolt and SiC remain unmeasured
---

# G-756 — Square-away

This is the one sheet. If a later sentence fights this sheet, this sheet wins until a measured node replaces it.

## Kernel (do not grow)

```text
1  Field / Void pairing          (ontology, not a count of choices)
2  binary choices                YES / NO                 BC
3  ternary moves                 DOWN / HOLD / UP         TC
4  views up, 4 actions down      B-206b / B-206c          QC
5  lifecycle verbs               Idle Primed Executing Vectoring Resolving
6  gates / routes                2 x 3 = 6                looped
6  Void mirrors                  descending copies of the same six, not six new axes
```

Matching a number across rows is not proof the rows are the same thing.

## Two rails, every step

Virtual ground `(0)` = HOLD = reference. Two rails sit around it. Every BC, TC, and QC step keeps both rails. No single-ended brain.

## Loop stacking (I to 2 to 3)

```text
1 loop   DC     BC polarity on the two rails          Field choice then Void choice
2 loops  AC     second loop makes the pair ternary    -(0)+
3 loops  QC     left or right connects the mirrored   views UP, spintronics / actions DOWN
```

That is G-755 plus G-740. Quadratic is how you *join* the mirrored loop, not a fourth choice.

## Timing names, not lobes

| Tag | Meaning |
|---|---|
| BC-DC | binary choice, direct / polarity |
| TC-AC | ternary, alternating around ground |
| QC-RC | quadratic, rotating; views up, actions down |

These name **when** a signal sits on the rails. They are not three brains.

## Brain versus nerve

Brain (G-753): Dream proposes, M4 routes, Administrator commits. Two Rubiks + midline (G-754).

Nerve: 3:1 compression, **three windings**, one virtual ground, bidirectional SiC MOSFET gates as the *proposed* body. mV preferred. M4 does not drive a winding. Admin writes ground. STOP = Void + HOLD on that ground.

```text
3 subconscious nerve flips  :  1 slow brain cycle
3:1 nerve compression       :  6:1 oversight / override alternating
```

6:1 is hex neighborhood + Admin/override pair, not a second ternary.

## Geometry on the cell, not extra physics

Sphere = bound. Cube = stacked chips. Double pyramid = hexagonal bipyramid (G-748). They nest. They are not three extra forces.

3 mirrors, 6 gates = three physical Mirror Gates × two orientations (Updated 33). Same six, not twelve instructions.

## Five scales

`micro small mid large macro` = C9 labels for PPF transfer. Not five more choices. Point / Path / Field rotation stays three kinematic roles at every scale (G-749/750, C2 done, C3 open).

## Threshold bands (declared, do not invent gaps)

Locked in `nested_rotation.py`:

```text
100-90  85-75  70-60  55-45  40-30  25-15  15-0
```

`15` is a shared boundary. A spoken `10-0` last band is **not** swapped in until a node replaces the 15-0 band on purpose.

## Two wrappers you were mixing (keep both, do not fuse)

**Intensity / thermal wrapper** (how hard the rail is loaded):

```text
fire  hot  warm  cool  cold  frozen
```

Maps onto threshold bands as a *display*, not a new axis.

**Tension wrapper** (what the move is doing to the lattice):

```text
compress / express     tension     release / snap
```

`>` and `-<` are signs on compress/express, not extra choices. HOLD is the quiet middle. Snap is Break if G-739 says Break. Do not also call snap "frozen." Frozen is thermal. Snap is tension.

If you only need one sentence: thermal is *how loaded*; tension is *which way the move pulls*.

## Spintronics down

QC actions down *may* be carried as spin/magnetic remnant (G-741 Yellow). Not implemented. Not Mass Effect. Not E5.

## What is still unbuilt (lock blockers)

- mV P0 cell that holds virtual ground and three windings
- mute brain cycle with no command string
- cube socket (same relation in and out)
- midline M4 bus timing
- C3 Path rotation
- SiC bidirectional gates as measured endurance, not slogan

## Build the computer from

Nerve cells that drive three windings. Brain cells that are only Dream/M4/Admin. Stack cell → chip → cube → Rubik → two Rubiks. Everything else is a wrapper or a measurement you do not have yet.
