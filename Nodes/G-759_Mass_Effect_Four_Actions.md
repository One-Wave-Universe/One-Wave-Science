---
id: G-759
title: Mass Effect as four-action carry
status: yellow-bridge
---

# G-759 — In, out, across, over

C-318 is the law. This node only names how the four actions push that law.

Mass Effect is **not** a stuff. It is the extra four-interaction work when a hold is moved relative to Ground and must be rebuilt.

## Four interactions (must all ride)

Knot. Electrical shell. Mirror relation. Boundary-Tension Weave. Plus crosses. Drop one and you are not allowed to call the residue Mass Effect.

## Four actions (B-206c)

| Action | What it does to the hold | Mass-Effect role |
|---|---|---|
| Inward | compress toward the shared `(0)` | loads knot + shell |
| Outward | express / release from `(0)` | unloads, can radiate |
| Across | meet at one boundary | two sides, one Ground |
| Over | force the orientation across a seam | *not* Mass Effect; that is the Mirror-Gate path |

Up / down on the nerve is ternary move around virtual ground. It is not a fifth action. In/out are the radial pair. Across/over are the boundary pair.

## Two derivatives of one energy

```text
small In/Out/Across inside the basin, rebuild the pattern at a new X
  -> M_ij = d^2 E4 / dv_i dv_j     Mass Effect

Over the first mirrored seam
  -> E_MG = E4(q_G) - E4(q_0)      gate, C-322
```

`m_eff != E_MG / c^2` as cause.

Drag `C v` is damping. Not mass.

## What is still missing to make it "real" in kilograms

The work metric `W` (4x4 blocks, crosses on). Without `W`, translating the seven-cell hold only moves numbers. With `W`,

`M_jk = sum (D_j Z)^T W (D_k Z)`.

Until `W` is derived from the update rule, One-Wave can be real as a *shape and a path*, not as a kg. G-757/758 are the shape and a sloppy path. They are not `W`.
