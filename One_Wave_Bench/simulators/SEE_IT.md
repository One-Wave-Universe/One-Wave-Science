# See it

Open `see_hold.html` in a browser. No install.

What you are watching:
- hex + center = nest (six pyramids flattened)
- lit vertex = which of the six process labels is nearest the angle
- orange bead = toy hold walking the two wells (`sin^2 theta`)
- Shove = extra kick toward the saddle. Hold = drop back near 0.

This is the toy E4 angle only. Engines in `field_void_recursive_sim.py` still have no camera. G-735 Micro freeze still applies: this visual is a prototype until a conserved engine drives it.
