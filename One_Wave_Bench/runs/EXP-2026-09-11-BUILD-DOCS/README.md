# EXP-2026-09-11-BUILD-DOCS — Progress Diary and Test Log

## MAIN GOAL

Build a reliable Field/Void software-construction engine for coding, app
building, and program building.

## Branch-step

- Current step: add the latest balanced-device build contracts and explicit
  development gaps.
- Base branch: `main`
- Base HEAD inspected: `e17c68d81eb9a61ba1afdddc451cbc72b16a305c`
- Task branch: `docs/balanced-builds-2026-09-11`
- Attempt: 1/3, documentation approach
- Hard start: repository identity, base HEAD, `AGENTS.md`, branch-step template,
  current build order, flashlight locks, and nerve/brain nodes inspected.

## Allowed change

Add documentation only for the balanced base cell, sensor cell, action/nerve
cell, brain cell, speaker, QC-RC rover/drone, status matrix, and this update
packet. Do not modify the editable breadboard implementation or claim new test
evidence.

## Field proposal

Add one coherent documentation package that separates cell responsibilities,
preserves reference/HOLD/authority rules, and lists the smallest next proof for
every unvalidated build.

## Void pre-oversight

Decision: `ALLOW` with these constraints:

- do not collapse brain and nerve;
- do not let an experimental layer bypass a flight controller;
- do not call a drawing, software shape, or VBB result a physical result;
- preserve `V0` as a reference rather than load-current return;
- preserve HOLD as no new correction, not forced zero output;
- preserve one bidirectional gate for Views Up and Actions Down while keeping
  their authority roles distinct;
- preserve three complete nerve cycles per one higher-brain function, with
  immediate preemption for fault/override;
- preserve existing flashlight and VBB work without edits.

## Checks

Planned static checks:

```text
- all manifest paths exist in the proposed tree
- Markdown files contain status/claims boundaries
- JSON manifest parses
- no existing file is modified by the packet
- branch differs from inspected main only by declared paths
```

No electrical simulation, bench test, rover drive, or drone flight was run in
this documentation step.

## Progress

- Completed: build contracts and explicit further-development sections.
- Working: existing repository files remain untouched by this additive packet.
- Not working/proven: all items marked unvalidated in the status matrix.
- Blocked: physical results require parts, fixtures, instrumentation, and
  experienced flight integration.

## Look-back

- What changed: the latest ideas are separated into testable build files.
- What worked: the current repo canon supplied stable reference, HOLD, nerve,
  and authority boundaries.
- What was not tested: runtime behavior and all new physical claims.
- What must carry forward: change one thing, test immediately, compare against
  a control, record differential, and stop at the declared hard boundary.

## Hard stop

Stop after the documentation branch and reviewable PR. Do not implement
hardware, change the Virtual Breadboard, purchase parts, or enable vehicle
outputs in this step.
