# Branch-Step 01 — State-Machine Anti-Drift Control

## MAIN GOAL

Convert active state-machine behavior into canonical watchdog nodes while preserving established semantics and producing evidence for every move.

## CURRENT STEP GOAL

Create the anti-drift control node, receipt template, telemetry location, and notes-to-self before any bulk conversion.

## HARD START

- Base branch: `canon/five-field-states`
- Working branch: `nodes/state-machine-anti-drift-controls`
- Read `AGENTS.md`, `BRANCH_STEP_PROJECT_TEMPLATE.md`, canonical state-machine architecture, and five-state correction.
- Protect the established Field lifecycle order.

## ONE CHANGE

Add the state-machine nodesification control layer only.

## SUCCESS CRITERIA

- The watchdog node contains every required control file.
- Receipt and notes-to-self requirements are explicit.
- Field lifecycle, modulation, and six-step recursion remain distinct.
- No active state-machine definition is rewritten in this step.

## TESTS / CHECKS

- Verify all required paths exist on the branch.
- Parse YAML control files.
- Confirm exact lifecycle order.
- Confirm the hard stop forbids bulk conversion.

## ANTI-DRIFT RECEIPT

- Intended change: add control layer only.
- Drift found: none intended; verify from branch diff.
- Protected behavior: all existing state-machine semantics.
- Next permitted action: inventory active state-machine sources in a new bounded branch-step.

## FIELD NOTES

The repository had general branch controls and integrity receipts but no state-machine-specific `.node` watchdog control folder.

## VOID NOTES

Do not authorize mass conversion until the control folder passes structural checks and the source inventory is receipted.

## HARD STOP

Stop when the control node and templates are committed and verified. Do not nodesify the state-machine content in this branch-step.
