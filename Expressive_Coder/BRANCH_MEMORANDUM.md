# BRANCH MEMORANDUM — STEP 00 MASTER PLAN

## COMPLETE PROJECT SCOPE
Build the actual C++ Field/Void state-machine coding engine for autonomously building, modifying, testing, debugging, and improving real software, applications, and programs. The deterministic CPU implementation is the semantic reference. Field is the expressive software-building side. Void is the differential oversight/override side. The canonical mirrored gates are F1/V6, F2/V5, F3/V4, F4/V3, F5/V2, F6/V1. Gate 4 is also the 0 mirror/null/flip boundary. Movement is strict ternary Compress=-1, Hold=0, Expand=+1. Six steps are Begin, Build, Hold, Build, Break, Loop. Five bands are Micro/Floor, Small/Low, Medium/Mid, Large/High, Macro/Ceiling. Four views are Inward, Outward, Across, Over. Choices are Nothing and Everything. The final loop is goal -> reference -> Field candidate -> Void differential -> ALLOW/CORRECT/OVERRIDE/HOLD/ESCALATE -> ternary next movement -> state/memory update -> code/test/diff evidence -> reference update -> loop. Every coding project must be planned from beginning to end first, broken into branches, and coded one branch at a time.

## COMPLETE ACCUMULATED JOURNAL HISTORY
Initial project journal: project created as a preplanned branch-by-branch C++ build. Gate semantics must be proven before neural/model/GPU behavior. CPU deterministic correctness is the reference. No implementation branch may invent its own purpose. Repository memory, not conversation memory, is authoritative.

## THIS BRANCH IN THE CODING PROJECT
Step 00 of 16 implementation stages. This branch exists only to lock the complete plan and create all reserved implementation branches.

## BEGINNING-TO-END CODING PLAN FOR THIS BRANCH
1. Write the full project scope.
2. Lock canonical gate and primitive semantics.
3. Lock CPU/GPU responsibility.
4. Define every implementation branch from Step 01 through Step 16.
5. Give every branch an explicit internal build sequence, hard start, tests, and hard stop.
6. Create every planned branch ahead of implementation.
7. Create branch memorandum rules and initial journal/progress references.
8. Verify branch names and plan consistency.

## HARD START
Repository readable; `main` exists; no implementation work has begun under this new Expressive Coder plan.

## SUCCESS TEST
The master plan exists and every planned branch 01-16 exists with a fixed purpose.

## HARD STOP
Stop when the full plan and all branches are created. Do not implement gate code here.

## FORBIDDEN FUTURE WORK
No C++ gate implementation, Field generation, Void differential, repo editing, recovery, GPU, or trial behavior.

## NEXT BRANCH
`expressive-coder/01-gate-primitives`
