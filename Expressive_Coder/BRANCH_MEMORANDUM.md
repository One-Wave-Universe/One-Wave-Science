# BRANCH MEMORANDUM — STEP 04 STATE PACKET

## COMPLETE PROJECT SCOPE
Build the C++ Field/Void state-machine coding engine for autonomous software/app/program building. CPU C++ is the semantic reference. Field expresses candidate software movement; Void performs differential oversight/override; M4/controller routes the loop. Canonical mirror pairs, Gate 4/0 boundary meaning, ternary movement, six steps, five bands, four views, two choices, and one shared Field/Mirror/Void reference remain fixed. Every project is planned completely before coding and implemented one reserved branch at a time.

## COMPLETE ACCUMULATED JOURNAL HISTORY
Initial journal: master plan locked. Steps 01-03 are reserved for primitives, mirror pairing, and deterministic transitions. Later stages must inherit those semantics without redefining them.

## THIS BRANCH IN THE CODING PROJECT
Step 04. Owns the validated shared state packet carried through Field, Void, memory, and later code-project operations.

## BEGINNING-TO-END CODING PLAN
1. Load tested gate/cycle code.
2. Define `EngineState`/state packet.
3. Store synchronized Field/Void gate pair and cycle step.
4. Add ScaleBand, View, Move, Choice.
5. Add project ID, task/reference ID, known-good ID, candidate ID.
6. Add attempt number, max attempts, and approach ID.
7. Add validation that gate pair remains mirrored and ternary values remain legal.
8. Add deterministic equality and printable representation for tests/logs.
9. Add serialization-ready field layout without implementing persistence yet.
10. Test valid construction, copy/equality, invariant failure, and representative state combinations.
11. Re-run Steps 01-03 tests.

## HARD START
Steps 01-03 pass; transition semantics are fixed; persistence is not implemented.

## SUCCESS TEST
State packets preserve all required control dimensions, enforce invariants, and compare deterministically.

## HARD STOP
Stop once packet tests and all earlier tests pass.

## FORBIDDEN FUTURE WORK
Field proposal logic, Void differential/policy, disk persistence, repository reads/edits, GPU.

## NEXT BRANCH
`expressive-coder/05-field-expression`
