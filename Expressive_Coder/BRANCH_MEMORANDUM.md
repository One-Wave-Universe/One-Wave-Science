# BRANCH MEMORANDUM — STEP 10 CODE PROJECT CONTEXT

## COMPLETE PROJECT SCOPE
Build the C++ Field/Void state-machine coding engine for autonomous software/app/program building. CPU C++ is the semantic reference. Field expresses one bounded candidate; Void performs differential oversight/override; M4/controller routes the loop. Canonical gate semantics, strict ternary movement, six steps, five bands, four views, two choices, and one shared Field/Mirror/Void reference remain fixed. Every project is fully planned before coding and implemented one pre-created branch at a time.

## COMPLETE ACCUMULATED JOURNAL HISTORY
Initial journal: plan locked before implementation. Steps 01-09 establish deterministic Field/Void behavior and persistent state. Repository access begins read-only here.

## THIS BRANCH IN THE CODING PROJECT
Step 10. Owns read-only reconstruction of a real code project into a bounded context packet.

## BEGINNING-TO-END CODING PLAN
1. Accept configured project root with `$HOME/One-Wave-Science` as current default.
2. Resolve and verify actual Git repo root.
3. Read active branch, HEAD, worktree status, and known-good reference.
4. Discover source, test, build, and configuration files relevant to the current task.
5. Build a bounded `CodeProjectContext` packet.
6. Include project goal, branch contract, and plan version.
7. Prove discovery and reading are read-only.
8. Test fixture repo reconstruction.
9. Byte/working-tree compare before vs after inspection.
10. Re-run Steps 01-09 tests.

## HARD START
Persistent engine state works; correct project root can be configured; no editing behavior exists.

## SUCCESS TEST
The engine reconstructs bounded project context and leaves the repository unchanged.

## HARD STOP
Stop after read-only invariants and previous tests pass.

## FORBIDDEN FUTURE WORK
Any file edit, commit, test-command mutation, retry implementation, GPU.

## NEXT BRANCH
`expressive-coder/11-code-action`
