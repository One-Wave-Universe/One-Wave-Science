# BRANCH MEMORANDUM — STEP 14 CPU GPU ADAPTER

## COMPLETE PROJECT SCOPE
Build the C++ Field/Void state-machine coding engine for autonomous software/app/program building. CPU C++ is the semantic reference. Field expresses one bounded candidate; Void performs differential oversight/override; M4/controller routes the loop. Canonical gate semantics, strict ternary movement, six steps, five bands, four views, two choices, and one shared Field/Mirror/Void reference remain fixed. Every project is fully planned before coding and implemented one pre-created branch at a time.

## COMPLETE ACCUMULATED JOURNAL HISTORY
Initial journal: plan locked before implementation. Steps 01-13 establish the entire deterministic CPU loop including recovery. GPU work is forbidden until those semantics are stable.

## THIS BRANCH IN THE CODING PROJECT
Step 14. Owns the replaceable compute backend boundary and CPU/GPU equivalence tests. GPU may accelerate vector/candidate/differential work but may not redefine state-machine semantics.

## BEGINNING-TO-END CODING PLAN
1. Define a provider-neutral `ComputeBackend` interface.
2. Wrap existing CPU reference calculations behind that interface without changing results.
3. Define optional GPU backend entry points for batch candidate vectors and differential calculations.
4. Add CPU fallback when GPU is unavailable.
5. Keep gate transition, Void decision, persistence, repo safety, and branch-control semantics on the CPU reference path.
6. Build deterministic fixture batches.
7. Compare CPU backend outputs with GPU backend outputs for the same fixtures within exact/tolerated numeric rules.
8. Prove switching backends requires no controller rewrite.
9. Test fallback behavior.
10. Re-run Steps 01-13 tests.

## HARD START
Complete CPU recovery loop passes; no unresolved semantic ambiguity remains in gate or decision behavior.

## SUCCESS TEST
CPU remains authoritative, optional GPU backend can be swapped in for approved parallel work, and fixture results agree with CPU semantics.

## HARD STOP
Stop after backend equivalence/fallback tests and all prior tests pass.

## FORBIDDEN FUTURE WORK
Sacrificial autonomous coding trial, real project expansion, redefining gate semantics inside GPU kernels.

## NEXT BRANCH
`expressive-coder/15-autonomous-coder-trial`
