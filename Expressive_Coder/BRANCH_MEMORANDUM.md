# BRANCH MEMORANDUM — STEP 09 MEMORY REFERENCE

## COMPLETE PROJECT SCOPE
Build the C++ Field/Void state-machine coding engine for autonomous software/app/program building. CPU C++ is the semantic reference. Field expresses one bounded candidate; Void performs differential oversight/override; M4/controller routes the loop. Canonical gate semantics, strict ternary movement, six steps, five bands, four views, two choices, and one shared Field/Mirror/Void reference remain fixed. Every project is fully planned before coding and implemented one pre-created branch at a time.

## COMPLETE ACCUMULATED JOURNAL HISTORY
Initial journal: plan locked before implementation. Steps 01-08 define and integrate the deterministic CPU Field/Void loop. Persistence begins only after that loop is known-good.

## THIS BRANCH IN THE CODING PROJECT
Step 09. Owns durable engine state, reference identity, journal events, attempt state, and restart continuity.

## BEGINNING-TO-END CODING PLAN
1. Choose a simple explicit persistent format for EngineState and LoopEvidenceRecord.
2. Implement state save.
3. Implement state load.
4. Persist project/branch/plan identifiers.
5. Persist known-good and candidate reference identifiers.
6. Persist attempt number, max attempts, and approach ID.
7. Append immutable journal events rather than rewriting history.
8. Detect corrupt or incomplete persisted state.
9. Test save -> process restart simulation -> exact reload.
10. Test invalid/corrupt state rejection.
11. Re-run Steps 01-08 tests.

## HARD START
One complete deterministic CPU Field/Void cycle passes; persistence is absent.

## SUCCESS TEST
Exact engine state and journal continuity survive restart and corrupt state is rejected safely.

## HARD STOP
Stop after restart-continuity tests and earlier tests pass.

## FORBIDDEN FUTURE WORK
Repository discovery, code edits, shell test execution, retry policy changes, GPU.

## NEXT BRANCH
`expressive-coder/10-code-project-context`
