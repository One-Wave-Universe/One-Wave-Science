# BRANCH MEMORANDUM — STEP 08 FIELD VOID LOOP

## COMPLETE PROJECT SCOPE
Build the C++ Field/Void state-machine coding engine for autonomous software/app/program building. CPU C++ is the semantic reference. Field expresses one bounded candidate software movement; Void compares it against the shared reference and controls the next movement with ALLOW/CORRECT/OVERRIDE/HOLD/ESCALATE. Canonical mirrored gates, Gate 4/0 boundary meaning, strict ternary movement, six steps, five bands, four views, two choices, and one shared Field/Mirror/Void reference remain fixed. Every project is fully planned before coding and implemented one pre-created branch at a time.

## COMPLETE ACCUMULATED JOURNAL HISTORY
Initial journal: plan locked before implementation. Steps 01-07 separately own primitive types, mirror mapping, cycle transition, shared state, Field candidate contract, Void differential evidence, and Void decision policy. Integration begins only here.

## THIS BRANCH IN THE CODING PROJECT
Step 08. Owns one complete deterministic CPU Field/Void cycle.

## BEGINNING-TO-END CODING PLAN
1. Create loop input from EngineState + goal/reference fixture.
2. Call Field exactly once.
3. Capture exactly one CandidateMovement.
4. Call Void differential exactly once.
5. Call oversight/override policy exactly once.
6. Apply ALLOW or OVERRIDE ternary movement through tested transition code.
7. Leave state unchanged on HOLD.
8. Mark correction-required state on CORRECT without hidden second edit.
9. mark terminal state on ESCALATE.
10. Produce a structured `LoopEvidenceRecord` containing before state, candidate, differential, decision, and after state.
11. Test all five decision paths end-to-end.
12. Re-run Steps 01-07 tests.

## HARD START
Steps 01-07 pass independently; no persistence or repository code behavior exists.

## SUCCESS TEST
One complete Field/Void cycle executes deterministically for every decision outcome.

## HARD STOP
Stop after one-cycle integration tests and earlier tests pass.

## FORBIDDEN FUTURE WORK
Persistent memory, local repo access, code edits, test command execution, retries, GPU.

## NEXT BRANCH
`expressive-coder/09-memory-reference`
