# BRANCH MEMORANDUM — STEP 05 FIELD EXPRESSION

## COMPLETE PROJECT SCOPE
Build the C++ Field/Void state-machine coding engine for autonomous software/app/program building. CPU C++ is the semantic reference. Field expresses one bounded candidate software movement from the current goal/reference/state. Void later measures and controls that movement. Canonical mirror pairs, Gate 4/0 boundary semantics, ternary movement, six steps, five bands, four views, two choices, and one shared Field/Mirror/Void reference remain fixed. Every project is fully planned first and coded one pre-created branch at a time.

## COMPLETE ACCUMULATED JOURNAL HISTORY
Initial journal: plan locked before implementation. Steps 01-04 define primitive semantics, mirrored switching, cycle transitions, and the shared state packet. Those semantics are inherited and cannot be silently redefined here.

## THIS BRANCH IN THE CODING PROJECT
Step 05. Owns Field's expressive candidate interface only. It does not approve candidates or perform Void logic.

## BEGINNING-TO-END CODING PLAN
1. Load validated EngineState.
2. Define `FieldInput` carrying project goal, current task, reference state, and bounded code context handle.
3. Define `CandidateMovement` carrying requested ternary Move, intended state changes, expected files/scope, expected behavior, preserved invariants, and exact success test.
4. Implement a deterministic fake/reference Field generator for fixtures.
5. Enforce exactly one candidate per pass.
6. Reject candidates missing scope, invariants, expected result, or test.
7. Forbid any `approved=true` or architecture-verdict field in Field output.
8. Test Expand/Hold/Compress candidate fixtures.
9. Test invalid multi-change/unbounded candidate rejection.
10. Re-run Steps 01-04 tests.

## HARD START
Steps 01-04 pass; valid shared state exists; no Void differential/policy exists.

## SUCCESS TEST
Field produces exactly one structurally valid bounded candidate and cannot self-approve.

## HARD STOP
Stop after candidate contract tests and previous tests pass.

## FORBIDDEN FUTURE WORK
Void differential, ALLOW/CORRECT/OVERRIDE policy, code editing, persistence, GPU.

## NEXT BRANCH
`expressive-coder/06-void-differential`
