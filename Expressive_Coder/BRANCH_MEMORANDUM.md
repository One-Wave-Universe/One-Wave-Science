# BRANCH MEMORANDUM — STEP 06 VOID DIFFERENTIAL

## COMPLETE PROJECT SCOPE
Build the C++ Field/Void state-machine coding engine for autonomous software/app/program building. CPU C++ is the semantic reference. Field expresses one bounded candidate; Void compares candidate and evidence against the shared goal/reference and produces differential evidence that later drives oversight/override. Canonical mirrored gates, Gate 4/0 boundary meaning, ternary movement, six steps, five bands, four views, two choices, and the one shared Field/Mirror/Void reference remain fixed. Every project is fully planned first and coded one pre-created branch at a time.

## COMPLETE ACCUMULATED JOURNAL HISTORY
Initial journal: plan locked before implementation. Steps 01-05 define primitives, mirror switching, cycle transition, shared state, and Field candidate structure. Those are inherited unchanged.

## THIS BRANCH IN THE CODING PROJECT
Step 06. Owns only Void's differential evidence calculation. It does not yet choose ALLOW/CORRECT/OVERRIDE/HOLD/ESCALATE.

## BEGINNING-TO-END CODING PLAN
1. Define `ReferenceEvidence` and `DifferentialEvidence`.
2. Compare candidate requested state against current reference state.
3. Compare declared scope against actual/available scope evidence.
4. Record missing required evidence.
5. Detect invariant conflict or regression flags.
6. Compute deterministic bounded differential values/flags without applying policy.
7. Preserve raw evidence separately from interpretation.
8. Test exact-match fixture.
9. Test scope expansion, regression, missing evidence, and state conflict fixtures.
10. Re-run Steps 01-05 tests.

## HARD START
Field candidate contract passes; candidate and reference structures are available; no decision policy exists.

## SUCCESS TEST
Void differential evidence is deterministic and correctly distinguishes matching and conflicting fixtures.

## HARD STOP
Stop after differential fixtures and all previous tests pass.

## FORBIDDEN FUTURE WORK
Decision policy, state mutation, code editing, retry loop, persistence, GPU.

## NEXT BRANCH
`expressive-coder/07-oversight-override`
